package com.gp.socialapp.data.material.source.remote

actual class FileManagerImpl : FileManager {
    override suspend fun saveFile(file: ByteArray): String {
        TODO("Not yet implemented")
    }
}